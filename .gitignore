node_modules/
dist/
*.log
.DS_Store
.env
.env.*
# harness runtime state (sessions + fine-tuning JSONL live under the user home dir by default,
# but ignore any local copies written into the repo during dev)
.qwen-harness/
sessions/
*.jsonl
